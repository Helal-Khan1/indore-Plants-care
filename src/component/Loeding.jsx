import React from "react";
import { ClipLoader } from "react-spinners";

const Loeding = () => {
  return (
    <div className=" min-h-screen flex items-center justify-center">
      <ClipLoader />
    </div>
  );
};

export default Loeding;
